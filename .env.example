# Dev server port (defaults to 5173 if unset)
PORT=5173

# Base path the app is served from (defaults to "/")
BASE_PATH=/

# Backend URL used by the Vite dev proxy for /api requests
# (defaults to http://localhost:3001 if unset)
VITE_API_BASE_URL=http://localhost:3001
